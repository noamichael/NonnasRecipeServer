import { PopoverKeyboardDirective } from './popover-keyboard.directive';

describe('PopoverKeyboardDirective', () => {
  it('should create an instance', () => {
    const directive = new PopoverKeyboardDirective();
    expect(directive).toBeTruthy();
  });
});
